package com.exceptionhandlers.avante_ar.dataClasses

//Probably temporary
//Just so a user object can be sent alongside the layout data
data class UserInfo(
    val userId : String,
    val title : String
)
